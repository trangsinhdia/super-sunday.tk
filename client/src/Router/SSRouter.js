import React, { Component } from 'react';
import {BrowserRouter as Router, Route, Link} from 'react-router-dom';

class SSRouter extends Component {
    render() {
        return (
            <Router>
                <Route></Route>
            </Router>
        );
    }
}

export default SSRouter;